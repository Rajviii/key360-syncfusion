import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.key360.syncfusion',
  appName: 'key360-syncfusion',
  webDir: 'public'
};

export default config;
